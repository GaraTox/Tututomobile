import React from "react";
import Header from '../Header';
import Admin from '../Admin';

function admin() {
  return (
    <section>
        <Header/>
        <Admin/>
    </section>
  );
}

export default admin;