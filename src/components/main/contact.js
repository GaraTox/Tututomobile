import React from "react";
import Header from '../Header';
import Contact from '../Contact';

function contact() {
  return (
    <section>
        <Header/>
        <Contact/>
    </section>
  );
}

export default contact;