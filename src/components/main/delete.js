import React from "react";
import Header from '../Header';
import Delete from "../Delete";

function deleted() {
  return (
    <section>
        <Header/>
        <Delete/>
    </section>
  );
}

export default deleted;