import React from "react";
import Header from '../Header';
import Home from '../Home';

function home() {
  return (
    <section>
        <Header/>
        <Home/>
    </section>
  );
}

export default home;
