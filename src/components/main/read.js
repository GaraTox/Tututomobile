import React from "react";
import Header from '../Header';
import Read from "../Read";

function read() {
  return (
    <section>
        <Header/>
        <Read/>
    </section>
  );
}

export default read;