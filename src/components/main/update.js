import React from "react";
import Header from '../Header';
import Update from "../Update";

function update() {
  return (
    <section>
        <Header/>
        <Update/>
    </section>
  );
}

export default update;